{
  "name": "task-manager",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run copy:legacy && npm run dev --workspace @task-manager/web",
    "build": "npm run copy:legacy && npm run build --workspace @task-manager/web",
    "test": "npm run test --workspace @task-manager/shared",
    "copy:legacy": "node scripts/copy-legacy.mjs"
  }
}
